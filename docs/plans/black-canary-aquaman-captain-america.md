# Plan: actualización integral y revisión completa de donde-esta-tu-local

> **Estado de ejecución** (actualizado 2026-08-05):
>
> - **Fase 1 (T1-T8): COMPLETADA Y COMMITEADA.** Helper `membership.ts` creado (parte de T19 adelantada). Notas: endpoints de miembros estaban en `src/routes/gang/`, no bajo `update/`; `refuseMember` ya escribe `REFUSED`; la CSP solo se aplica fuera de dev y con `'unsafe-inline'` en `script-src` (mejorable con nonces); los endpoints de miembros aún devuelven el objeto `user` completo en el JSON (PII en respuesta, recortable).
> - **Fase 2 (T9-T12): COMPLETADA Y COMMITEADA.** T9 ya estaba resuelto en Fase 1 (pendiente mejora opcional: borrar blob antiguo del avatar). **T10 APLAZADO por decisión del usuario**: código ya unificado a `REFUSED` (no queda ningún `REJECTED` en `src/`); pendientes la migración SQL de normalización y los enums de Prisma.
> - **Fase 3: T13 COMPLETADA Y COMMITEADA** (todo minor/patch al día; prettier se queda en 3.6.2 porque ≥3.7 rompe prettier-plugin-svelte 3.x — se resuelve con el plugin v4 en T16; `globals` 16→17 es major, va a T15/T16). Cambios extra: `resolve()` en DockLink/Link (nueva regla eslint), `.codegraph/` en `.prettierignore`, `runtime: 'nodejs22.x'` en adapter-vercel. **T14: APLAZADO** — `@prisma/extension-accelerate` 3.x es incompatible con Prisma 7: pierde todos los tipos del cliente (issue [prisma/prisma#28580](https://github.com/prisma/prisma/issues/28580)), lo que rompe el type-check estricto. Se deja en Prisma 6.19.3 + Accelerate 2.0.2; se reintentará cuando exista una versión compatible.
> - **Fase 3: T15 COMPLETADA Y COMMITEADA** — actualizadas toolchain majors: Vite 8.2, `@sveltejs/vite-plugin-svelte` 7.2, `@sveltejs/adapter-vercel` 6.3, ESLint 10.8, `@eslint/js` 10, `@eslint/compat` 2.1, `globals` 17.9. Se adelantó `@sveltejs/enhanced-img` 0.11 porque la versión anterior 0.8.5 fallaba con Vite 8 (`Cannot read properties of undefined (reading 'id')`). TypeScript se mantiene en 5.9.3 por compatibilidad con `svelte-check` y `typescript-eslint`.
> - **Fase 3: T16 COMPLETADA Y COMMITEADA** — actualizadas runtime/libs majors: `@lucide/svelte` 1.28, `nodemailer` 9 + `@types/nodemailer` 8, `pino` 10.3, `flowbite` 4.0, `@vercel/analytics` 2.0, `@vercel/otel` 2.1, `@vercel/blob` 2.6, `@types/node` 26.1, `prettier` 3.9.6 + `prettier-plugin-svelte` 4.1.1 + `prettier-plugin-tailwindcss` 0.8.1. `flowbite-svelte` 1.33 sigue funcionando con `flowbite` 4. `@sveltejs/enhanced-img` se subió en T15. Tras el build hay que reformatear `src/lib/paraglide/**` con prettier.
> - **Fase 3: T17 COMPLETADA Y COMMITEADA** — `bun audit` pasó de 26 vulnerabilidades (1 crítica en `node-tar`) a 0 vía `overrides` (tar, postcss, defu, sharp, validator, rollup, flatted, picomatch) y declarando explícitamente la cadena `@opentelemetry/*` que `@vercel/otel` 2.x exige (antes resuelta a la 1.x transitiva). `bun outdated` solo lista los majors ya aplazados (Prisma 7, TypeScript 7).
> - **Fase 5: T27 COMPLETADA Y COMMITEADA** (adelantada) — borrado el legado de la migración D1/Auth0: scripts `prisma/migrate-*.ts`, `seed-activities-staging.ts`, `auth-minimal.ts`, `wrangler.toml`, ruta `/notices` y restos comentados en `routes.ts`/`coords-monte.ts`; desinstalados `auth0` y `sqlite3`; `AGENTS.md`/`README.md` actualizados.
> - **Fase 4 (T18-T23) y Fase 6 (T28): COMPLETADAS Y COMMITEADAS.** Ver detalle en cada tarea más abajo.
> - **PLAN COMPLETO.** Las únicas tareas aplazadas a conciencia son T10 (migración SQL + enums de Prisma para `REJECTED`/`REFUSED`) y T14 (Prisma 7, bloqueado por `@prisma/extension-accelerate`); ambas documentadas en su tarea y en `AGENTS.md`.
> - Verificación vigente: `bun run check` (0 errores, 11 warnings de Svelte 5.56 tipo `state_referenced_locally`), `bun run lint` exit 0 con 5 warnings por `eslint-disable` no usados, `bun run only-build` exit 0, `bun audit` 0 vulnerabilidades. OJO: usar `only-build`, nunca `build` (tocaría la BD). Revisión manual en `bun run dev` (T28) con Playwright/chromium contra la BD real: mapa, filtro, modal de login, `gang/add` (con y sin sesión), `activities` (carteles bajo demanda incl. modal con imagen grande), `profile` y las 4 páginas de `/admin` devuelven 401 "Usuario no conectado" sin sesión (esperado); sin errores de consola en ningún flujo.

Decisiones del usuario: **actualizaciones agresivas** (incluye majors como Prisma 7, Vite 8, ESLint 10), **eliminar legado** de la migración D1/Auth0, y **arreglos + refactor completo** de código.

Hallazgos de partida (auditoría previa): 1 IDOR alto, XSS almacenado en popup del mapa, endpoints GET que mutan, bypass de moderación, rate-limit OTP solo en cliente, PII expuesta, sin cabeceras de seguridad, bugs de datos (avatar, REJECTED/REFUSED), texto hardcodeado masivo (toda `/admin`), duplicación de Leaflet ×4, y ~45 dependencias desactualizadas.

Verificación transversal: tras cada fase, `bun run check` y `bun run lint` deben pasar. Al final, `bun run format` + `bun run only-build`. No hay tests; la barrera es check+lint+build. (Nota: el plan original decía npm; el proyecto usa **bun**.)

---

## Fase 1 — Seguridad (crítica, antes de tocar dependencias)

- [x] **T1. IDOR en edición de peñas** — `src/routes/gang/[slug]/update/+page.server.ts:42-117`: la action solo exige login. Añadir comprobación de que el usuario es miembro `VALIDATED` de esa peña o `role` admin/system; devolver 403 si no. Reusar la lógica del layout (extraída en T19).
- [x] **T2. XSS almacenado en popup del mapa** — `src/routes/+page.svelte:32-34`: `bindPopup` recibe HTML con `gang.name` sin sanitizar. Construir el popup con `document.createElement` + `textContent` (patrón ya usado en `gang/add/+page.svelte:51-59`).
- [x] **T3. Endpoints de miembros GET→POST y endurecerlos** — `gang/addMember`, `validateMember`, `refuseMember` (`+server.ts`):
  - Pasar los tres a `POST` y actualizar `src/lib/components/ButtonRequest.svelte` (`fetch(..., { method: 'POST' })`).
  - Validar `gangId`/`userId` con zod (evita 500 por `NaN`).
  - Códigos coherentes: 401 sin sesión, 403 sin permisos (hoy devuelve 404).
  - `validateMember`: admitir bypass también para `role === 'system'` (hoy solo admin).
  - `refuseMember`: añadir bypass admin/system (la UI admin lo invoca y hoy da 403).
  - `addMember`: rechazar si el estado actual es `REFUSED` (un rechazado no puede re-solicitar), y exigir que la peña exista y esté `VALIDATED`.
- [x] **T4. Rate limit real del OTP** — `src/lib/server/auth.ts`: configurar `rateLimit` de better-auth (estricto en rutas `email-otp`, p.ej. 3/min por IP). Simplificar `api/user/email-sent-check/+server.ts` (hay lógica inalcanzable en :47-60) y dar respuesta uniforme para no permitir enumeración de cuentas/sesiones.
- [x] **T5. PII en datos serializados** — `gang/[slug]/+page.server.ts:20-31`: quitar `email` del `select` de miembros (la UI solo usa name/image); devolver `pendingMembers` solo si el visitante es miembro validado o admin.
- [x] **T6. Cabeceras de seguridad** — nuevo handle en `src/lib/handles/security-headers-handle.ts` (primero en la secuencia de `src/hooks.server.ts`): `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, `frame-ancestors 'self'`, `Permissions-Policy` restrictiva y CSP (tras T18 el CSS de Leaflet irá en bundle; orígenes necesarios: `tile.openstreetmap.org`, blob de Vercel, data:/blob: para mapa e imágenes).
- [x] **T7. Logs sin PII** — loguear solo ids en `addMember/+server.ts:73`, `validateMember/+server.ts:113`, `refuseMember/+server.ts:102`, `gang/add/+page.server.ts:22`, `update/+page.server.ts:51`. Mantener el OTP solo en `debug` (ya correcto).
- [x] **T8. Subida de avatar** — `profile/+page.server.ts:72-73`: derivar la extensión del MIME validado por zod (allowlist), no de `imageFile.name`; fijar `contentType` del blob desde el MIME validado.

## Fase 2 — Bugs de datos y robustez

- [x] **T9. Avatar borrado al editar perfil** — `profile/+page.server.ts:101-110`: incluir `image` en el update solo cuando se suba imagen nueva (hoy `null` pisa el avatar existente). Opcional: borrar el blob antiguo de Vercel Blob al sustituirlo (comentario pendiente en :89-91).
- [ ] **T10. Unificar estados REJECTED/REFUSED + enums** — **APLAZADO por decisión del usuario (2026-08-05)**: el código ya está unificado a `REFUSED`; quedan pendientes la migración SQL para normalizar filas existentes y la conversión de `membershipGangStatus`, `Gang.status` y `GangHistory.changeType` a enums de Prisma (con migración).
- [x] **T11. Slugs no numéricos → 404** — validar `parseInt`/`Number.isNaN` en `gang/[slug]/+page.server.ts:17`, `update/+page.server.ts:20`, `update/+layout.server.ts:29` y devolver `error(404)` antes de la query.
- [x] **T12. Fragilidad en cliente** — `FormUser.svelte:36-40`: `messageClass` por matching de texto → usar status de respuesta (patrón de `FormAddGang.svelte:57-61`). `FormLogin.svelte:79`: quitar `error as string` y renderizar mensaje seguro.

## Fase 3 — Actualización de dependencias (agresiva)

- [x] **T13. Minor/patch masivas** — actualizar a "wanted" todo lo demás: `@sveltejs/kit` 2.70, `svelte` 5.56, `tailwindcss`/`@tailwindcss/vite` 4.3, `better-auth` 1.6, `bits-ui` 2.18, `daisyui` 5.7, `flowbite-svelte` 1.33, `paraglide-js` 2.23, `superforms` 2.30, `svelte-check` 4.7, `tsx`, `prettier` 3.9 + plugins, `typescript-eslint` 8.66, `@types/*`, `pino-pretty`, `globals` 17, etc. Verificar check+lint+build tras cada tanda. **Nota de ejecución**: prettier se quedó en 3.6.2 (≥3.7 rompe prettier-plugin-svelte 3.x; se resuelve con plugin v4 en T16); `globals` 17 es major, aplazada a T15/T16.
- [ ] **T14. Prisma 7 (major)** — APLAZADO. `@prisma/extension-accelerate` 3.x pierde todos los tipos del cliente con Prisma 7 ([prisma/prisma#28580](https://github.com/prisma/prisma/issues/28580)), rompiendo el type-check estricto. Se mantiene Prisma 6.19.3 + Accelerate 2.0.2. Cuando haya compatibilidad, el plan de migración es: `prisma`/`@prisma/client` 7.9, generator `prisma-client` con output path, `prisma.config.ts` con `defineConfig`, `accelerateUrl` en el constructor y migrar todos los imports de `'@prisma/client'` al output generado.
- [x] **T15. Toolchain majors** — `vite` 8.2 + `@sveltejs/vite-plugin-svelte` 7.2 + `@sveltejs/adapter-vercel` 6.3; `eslint` 10.8 + `@eslint/js` 10 + `@eslint/compat` 2.1; `globals` 17.9. TypeScript se mantiene en 5.9.3 (no hay soporte estable para TS 7 en `svelte-check`/`typescript-eslint`). `@sveltejs/enhanced-img` se subió a 0.11 dentro de T15 porque 0.8.5 es incompatible con Vite 8. `oxlint` + `eslint-plugin-oxlint` ya estaban en 1.77.
- [x] **T16. Majors de runtime/libs** — `nodemailer` 9 + `@types/nodemailer` 8, `pino` 10.3 (sin cambios en `logger.ts`), `@lucide/svelte` 1.28 (imports compatibles), `flowbite` 4.0 (compatible con `flowbite-svelte` 1.33), `@vercel/analytics` 2.0, `@vercel/otel` 2.1, `@vercel/blob` 2.6 (API de `put` compatible), `@types/node` 26.1, `prettier` 3.9.6 + `prettier-plugin-svelte` 4.1.1 + `prettier-plugin-tailwindcss` 0.8.1. `@sveltejs/enhanced-img` 0.11 se aplicó en T15.
- [x] **T17. Auditoría final de deps** — `bun audit` (objetivo 0 vulnerabilidades), `bun outdated` vacío salvo lo aplazado conscientemente, `bun.lock` regenerado y `bun run only-build` verde.

## Fase 4 — Refactor y deuda técnica

- [x] **T18. Helper de Leaflet** — extraer `initMap()` en `src/lib/utils/` (import dinámico, tile layer + attribution) y usar `leaflet/dist/leaflet.css` desde el bundle; eliminar los 4 `<link>` a unpkg y la inicialización duplicada de `+page.svelte`, `gang/[slug]`, `gang/add`, `update`.
- [x] **T19. Helpers de servidor** — `$lib/server/membership.ts` con la lógica común de los 3 endpoints de miembros (~80% duplicado) y `requireAdmin(locals)` / `requireValidatedMember(locals, gangId)`; usarlos en T1/T3 y en las páginas admin (hoy repiten el check de rol con comportamientos distintos: 403 vs 401 vs redirect). `admin/+layout.server.ts` usa ahora `requireAdmin` (unifica a 401/403 en vez de redirect); `admin/gangs/+page.server.ts` usa `requireAdmin` en las actions `validate`/`refuse` (bypasan el layout load); los `load` de `gangs`, `members` e `history` quitan el check duplicado porque el layout ya protege la navegación.
- [x] **T20. Triple query en `/gang/[slug]/update`** — layout + página hacen la misma query con `include: members` (3 queries por navegación); el layout solo necesita `count` de membresía validada. Reducir a lo imprescindible y extraer el fallback nombre→email duplicado. El layout ya no trae la peña completa con miembros: reutiliza `canManageGangMembers` de `membership.ts` (T19), que solo hace un `findUnique` por el usuario actual y, si es admin/system, ni eso. Nuevo `$lib/utils/member-display.ts` (`memberDisplayName`/`memberInitial`) sustituye el `member.name || member.email` duplicado en `admin/gangs` y `admin/members`.
- [x] **T21. Tipado estricto** — eliminar `as any` de `src/lib/auth-client.ts:7` (o documentar motivo), tipar `FormFields.svelte:9` con genéricos de superforms, tipar `App.Locals.user` desde el tipo inferido de better-auth (incluye `role`), quitar casts innecesarios (`+layout.svelte:16` miente: `user` puede ser undefined → `UserPrisma | undefined`, propagar a `NavBarEnd`), limpiar casts redundantes en `profile`, `gang/[slug]`, `email-sent-check` y `better-auth-handle.ts:14-15`. `as any` de auth-client.ts era innecesario (se quitó sin más). `FormFields`/`FormString` ahora son componentes genéricos (`generics="T extends Record<string, unknown>"`), lo que también arregló mismatches de tipo latentes en `FormLogin`/`FormUser`/`FormAddGang`. `App.Locals` deriva de `auth.$Infer.Session` (requirió quitar las anotaciones de tipo que aplanaban `getBetterAuth`/`betterAuthHandle` a `BetterAuthOptions` genérico, perdiendo la inferencia de plugins). `+layout.svelte`/`NavBarEnd` ya no mienten sobre `user` pudiendo ser `undefined`; `profile/+page.svelte` usa `!` con comentario (el `+layout.svelte` del profile ya garantiza que existen); `GangData`/`Member` pasan de `as` a `satisfies` o se quitan por redundantes. `email-sent-check` ya no tenía casts (se limpiaron en T4).
- [x] **T22. i18n completo** — mover a `messages/es.json` todo el texto hardcodeado: páginas `/admin` enteras (+page, gangs, members, history, incl. mensajes de actions), páginas públicas (`+page.svelte`, `gang/[slug]`, `add`, `update`, `activities`, `profile`) y componentes (`FormLogin`, `Footer`, `ActivityCard`, `+layout`); asunto/cuerpo del email OTP en `auth.ts:46-47`. Limpiar claves huérfanas (`form_email`, `routes_login`, etc.), duplicados y erratas (`sessión`, `exito`, `intentelo`, `Socilitar`, `logado`) y unificar registro tú/usted. Recompilar con `bun run paraglide-js`.
  - ~154 strings migradas a ~140 claves nuevas en 22 ficheros (todo `/admin`, que no usaba `m` en absoluto, más el resto de páginas/componentes de la lista). 12 claves huérfanas eliminadas (varias del `/notices` que ya borró T27). Registro unificado a tú: "ingrese"/"inténtelo" → "introduce"/"inténtalo"; "logado"/"logueado" → "conectado". Bonus fuera de la lista pero mismo patrón: `show-my-position.ts` (console→logger ya cubierto en T26, aquí solo el texto), `$lib/routes.ts` (`'Admin'`, `'Admin Gangs'`, `'Admin Miembros'` hardcodeados), `member-display.ts` ('Sin nombre' → `common_no_name`), y `admin/history/+page.server.ts` paralelizó sus 3 counts con `Promise.all` (mismo hueco que T23, no listado allí).
  - Verificado: 0 claves huérfanas y 0 referencias a claves inexistentes (script de cross-check contra todo `src/`).
- [x] **T23. Performance** — `Promise.all` en queries secuenciales de `admin/gangs` y `admin/members`; `import.meta.glob` de carteles sin `eager` (o filtrado por `bannerPath`) en `activities/+page.svelte:15-23`; avatar fallback local en `NavBarEnd.svelte:39-40` (hoy img.daisyui.com); transport nodemailer cacheado a nivel de módulo en `sender.ts:20`; quitar `console.log` por fetch del service worker. Los carteles de actividades (algunos de varios MB) ahora se cargan bajo demanda con `{#await}` en vez de con `eager:true`. El avatar sin imagen usa la misma inicial-placeholder de `member-display.ts` (T20) en vez de una foto de stock externa.
- [x] **T24. Accesibilidad** — `type="button"` explícitos (`gang/[slug]/+page.svelte:103`, `+page.svelte:103`, `Modal.svelte:42`), `aria-label` en botón atrás de `Header.svelte:34` y en inputs de filtro, `alt` descriptivos en avatares. También `type="button"` en los otros botones sueltos de esas mismas páginas (imhere, compartir). `alt="Profile"` genérico sustituido por el nombre real en `MemberDetail.svelte` y `profile/+page.svelte`.
- [x] **T25. Store legacy → runes** — `src/lib/stores/loginModal.ts` de `writable` a `$state` en `.svelte.ts` (único store legacy; la migración a runes ya está completa en el resto). Nueva API: `loginModalStore.value` (getter/setter) en vez de `$loginModalStore`/`.set()`.
- [x] **T26. console.\* → logger** — `ButtonRequest.svelte:25,38`, `gang/[slug]/+page.svelte:55`, `admin/gangs/+page.server.ts:111,146`, `admin/history/+page.server.ts:67`. De paso, `show-my-position.ts` (mismo caso, no listado explícitamente): `logger` ya soporta navegador (`logger.ts` tiene `browserOptions`), así que también sustituye a `console.*` en componentes cliente.

## Fase 5 — Eliminación de legado

- [x] **T27. Borrar legado de migración** — `wrangler.toml`, `prisma/migrate-gangs.ts`, `prisma/migrate-users.ts`, `prisma/seed-activities-staging.ts` (si ya no se usa), `database.sql` si existe, `src/lib/server/auth-minimal.ts` (solo lo usan los scripts de migración), ruta vacía `src/routes/notices/` (+ bloque comentado en `src/lib/routes.ts:53-59` y coords comentadas en `coords-monte.ts:4`). Desinstalar `sqlite3` y `auth0` de devDependencies. Actualizar `AGENTS.md` (secciones de stack, estructura y comandos de migración) y `README.md` si las menciona.

## Fase 6 — Verificación final

- [x] **T28. Cierre** — `bun run format`, `bun run check`, `bun run lint`, `bun run only-build` todo en verde; `bun audit` sin vulnerabilidades; revisión manual rápida en `bun run dev` de flujos tocados (login OTP en log, mapa, alta/edición de peña, perfil con avatar, panel admin); `AGENTS.md` reflejando el estado final (comandos, estructura, convenciones nuevas como el helper de Leaflet y los handles). Revisión manual hecha con Playwright (Chromium del sistema, sin `chromium-cli` disponible) contra `bun run dev` con la BD real: capturas de portada (mapa+marcadores+filtro), modal de login, `gang/add` sin sesión, `activities` (pestaña "Pasadas" con carteles cargados bajo demanda y modal con imagen de varios MB abriéndose correctamente), y los 401 esperados en `/admin/*` sin sesión — 0 errores de consola/página en todos los casos. `AGENTS.md` actualizado: helper de Leaflet, orden real de los handles (faltaba `securityHeadersHandle`), `membership.ts`, `member-display.ts`, tipado de `App.Locals` desde better-auth, componentes genéricos, `bun` en vez de `npm` en los comandos, `overrides` de `package.json`, y las dos tareas aplazadas (T10, T14).

## Orden y dependencias

- Fases 1 y 2 van primero: son cambios pequeños sobre código conocido y cierran agujeros explotables hoy.
- Fase 3 después: el código ya saneado hace más fácil detectar roturas de las majors. T14 (Prisma 7) es la tarea con más riesgo; va aislada.
- Fases 4-5 con todo actualizado, para refactorizar sobre APIs finales.
- Cada tarea deja check+lint en verde antes de seguir; si una major rompe algo no subsanable, se aplaza esa dependencia y se documenta en el plan/AGENTS.md.
